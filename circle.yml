machine:
  java:
    version: oraclejdk7
  environment:
    GRADLE_OPTS: -Dorg.gradle.daemon=true  -Xmx1024m -Xms256m
    CI_OPTS: --stacktrace --debug

test:
  override:
    - ./gradlew -x signArchives
